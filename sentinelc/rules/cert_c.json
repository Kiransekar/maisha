{
  "standard": "CERT-C",
  "note": "Rule summaries are original paraphrases for tooling purposes. Consult the SEI CERT C Coding Standard for normative wording.",
  "rules": {
    "STR31-C": {"severity": "blocker", "summary": "Guarantee destination storage is large enough for string data plus the null terminator (gets/strcpy/strcat/sprintf are unbounded).", "fix": "Use bounded APIs (strncpy_s-style, snprintf) with correct size arguments and verify truncation.", "cross": ["MISRA Rule 21.6"]},
    "STR32-C": {"severity": "blocker", "summary": "Do not pass non-null-terminated character sequences to functions that expect strings.", "fix": "Ensure explicit null termination after bounded copies.", "cross": []},
    "MEM30-C": {"severity": "blocker", "summary": "Do not access memory after it has been freed.", "fix": "Null pointers immediately after free; audit ownership so each allocation has exactly one release path.", "cross": ["MISRA Rule 21.3"]},
    "MEM31-C": {"severity": "blocker", "summary": "Free dynamically allocated memory exactly once.", "fix": "Set pointers to NULL after free and guard release paths.", "cross": []},
    "MEM34-C": {"severity": "blocker", "summary": "Only free memory that was dynamically allocated.", "fix": "Never free stack, static, or offset pointers.", "cross": []},
    "MEM35-C": {"severity": "critical", "summary": "Allocate sufficient memory for an object, guarding size computations against overflow.", "fix": "Validate n and check n <= SIZE_MAX / sizeof(elem) before multiplying in allocation sizes.", "cross": ["INT30-C"]},
    "EXP33-C": {"severity": "blocker", "summary": "Do not read uninitialized memory.", "fix": "Initialize every object at its point of declaration.", "cross": []},
    "EXP34-C": {"severity": "blocker", "summary": "Do not dereference null pointers.", "fix": "Check pointers (including allocator returns) before every dereference.", "cross": []},
    "EXP39-C": {"severity": "critical", "summary": "Do not access an object through a pointer of an incompatible type.", "fix": "Use memcpy or properly typed interfaces instead of pointer type-punning.", "cross": ["MISRA Rule 11.3"]},
    "EXP45-C": {"severity": "major", "summary": "Do not perform assignments inside selection or iteration condition expressions.", "fix": "Move the assignment out of the condition.", "cross": ["MISRA Rule 13.4", "BARR-C 8.2a"]},
    "INT30-C": {"severity": "critical", "summary": "Ensure unsigned integer operations do not wrap unintentionally.", "fix": "Pre-check operands against limits from <stdint.h>/<limits.h> before arithmetic.", "cross": []},
    "INT31-C": {"severity": "critical", "summary": "Ensure integer conversions do not lose or misinterpret data.", "fix": "Range-check before narrowing or sign-changing conversions.", "cross": ["MISRA Rule 10.1"]},
    "INT32-C": {"severity": "critical", "summary": "Ensure signed integer operations do not overflow (undefined behavior).", "fix": "Guard arithmetic with explicit bound checks before the operation.", "cross": []},
    "INT33-C": {"severity": "critical", "summary": "Ensure division and remainder operations cannot divide by zero.", "fix": "Validate divisors before / and %.", "cross": []},
    "ARR30-C": {"severity": "blocker", "summary": "Do not form or use out-of-bounds pointers or array subscripts.", "fix": "Bound-check every index derived from input or computation.", "cross": []},
    "ARR32-C": {"severity": "critical", "summary": "Ensure size arguments for variable-length arrays are valid and bounded.", "fix": "Avoid VLAs; use fixed maximum-size arrays.", "cross": ["MISRA Rule 18.8"]},
    "FLP32-C": {"severity": "critical", "summary": "Prevent or detect domain and range errors in floating-point math functions.", "fix": "Validate math-function inputs and check errno/fetestexcept.", "cross": []},
    "FLP37-C": {"severity": "major", "summary": "Do not compare floating-point values with == or != for equality.", "fix": "Compare with an application-appropriate epsilon tolerance.", "cross": []},
    "FIO30-C": {"severity": "blocker", "summary": "Never pass externally influenced data as a printf-family format string.", "fix": "Use a literal format: printf(\"%s\", user_data).", "cross": []},
    "FIO34-C": {"severity": "critical", "summary": "Distinguish characters read from a file from EOF/WEOF.", "fix": "Store getc results in int, not char, before comparing to EOF.", "cross": []},
    "ERR33-C": {"severity": "critical", "summary": "Detect and handle standard library errors (check return values).", "fix": "Check every fallible call's return value and propagate errors.", "cross": ["MISRA Rule 17.7"]},
    "ERR34-C": {"severity": "critical", "summary": "Detect errors when converting strings to numbers (atoi cannot report failure).", "fix": "Use strtol/strtoul with endptr and errno checks.", "cross": ["MISRA Rule 21.7"]},
    "ENV33-C": {"severity": "blocker", "summary": "Do not invoke command processors via system().", "fix": "Call specific APIs (exec-family with fixed argv) or remove the shell-out entirely.", "cross": ["MISRA Rule 21.8"]},
    "SIG30-C": {"severity": "critical", "summary": "Call only async-signal-safe functions inside signal handlers.", "fix": "Set a volatile sig_atomic_t flag in the handler; do work in the main loop.", "cross": ["MISRA Rule 21.5"]},
    "CON33-C": {"severity": "critical", "summary": "Avoid race conditions from library functions that use static internal state.", "fix": "Use reentrant variants (strtok_r, localtime_r) in concurrent code.", "cross": []},
    "MSC32-C": {"severity": "critical", "summary": "Seed pseudorandom generators properly; do not use rand() where quality matters.", "fix": "Use a CSPRNG for anything security-relevant.", "cross": []},
    "MSC33-C": {"severity": "blocker", "summary": "Do not pass invalid data to asctime()/similar fixed-buffer time functions.", "fix": "Use strftime with a sized buffer.", "cross": ["MISRA Rule 21.10"]},
    "DCL30-C": {"severity": "blocker", "summary": "Do not let object addresses escape their lifetime (e.g., returning pointers to locals).", "fix": "Return by value, use caller-provided buffers, or static storage with documented semantics.", "cross": []},
    "DCL37-C": {"severity": "major", "summary": "Do not declare or define reserved identifiers (leading underscore, standard names).", "fix": "Rename identifiers to non-reserved forms.", "cross": ["BARR-C 7.1e"]},
    "PRE31-C": {"severity": "major", "summary": "Avoid side effects in arguments to unsafe (multi-evaluating) macros.", "fix": "Evaluate arguments into locals before invoking the macro, or convert the macro to a function.", "cross": []}
  }
}
